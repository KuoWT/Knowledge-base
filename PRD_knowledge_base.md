# 知識庫系統 PRD

## 1. 產品目標
- 建立一套以 `GitLab` 為唯一正式來源的知識庫系統。
- 支援兩種更新知識庫方式：
  - 人工編寫後經 `GitLab merge` 正式發布。
  - 使用者下指令，由 `agent` 協助整理 `md`。
- 正式索引與查詢內容以已合併版本為準。

## 2. 使用情境
- 工程師使用 `Obsidian` 撰寫知識內容。
- 使用者透過 `agent` 要求整理、改寫或補充既有內容。
- 內容經整理後，仍需進入 GitLab 正式版本控管流程。
- 系統在 merge 後自動更新向量索引，讓查詢端取得最新知識。

## 3. 內容範圍
- 主要格式為 `Markdown (.md)`。
- 附件可以附上，但不建立索引。
- GitLab repository 內的內容視為唯一正式版本。

## 4. 更新方式

### 4.1 方式 A：人工流程
- 工程師在 Obsidian 編寫或修改內容。
- Push 到 GitLab。
- Merge 到主分支後，觸發正式同步流程。

### 4.2 方式 B：Agent 協作流程
- User 下指令給 agent。
- agent 協助整理、改寫、補充 `md`。
- 產出內容後仍需進入 GitLab 正式流程。
- 最終以 merge 後版本作為正式內容。

## 5. 觸發規則
- 只在 `merge 到主分支` 後觸發正式知識庫更新。
- 不以一般 push 作為正式索引觸發點。
- 觸發來源為 `GitLab Webhook`。

## 6. 系統角色
- `Obsidian`：工程師編寫工具。
- `GitLab`：正式內容庫與版本控管中心。
- `agent`：協助整理 md 的內容助手。
- `Hermes`：流程協調與索引更新核心。
- `MCP`：Hermes 的寫入介面。
- `Qdrant`：向量索引儲存。

## 7. 功能需求
- 系統需接收 GitLab merge webhook。
- 系統需能辨識變更檔案。
- 系統需只處理 Markdown 內容進索引。
- 系統需支援新增、修改、刪除。
- 系統需將 Markdown 內容切 chunk 後建立 embedding。
- 系統需將向量與 metadata 寫入 Qdrant。
- 系統需保留失敗重試與任務狀態紀錄。

## 8. 非功能需求
- 支援增量更新，不做每次全量重建。
- 知識更新延遲需可控，避免 merge 後長時間不可查。
- 系統需可追蹤、可回溯、可重跑。
- 附件不建立索引，但需保留在 GitLab 版本中。

## 9. 驗收標準
- merge 到主分支後，Hermes 能正確收到 webhook。
- Hermes 能拉取最新內容並辨識變更檔案。
- 只有 `.md` 會進入索引流程。
- 附件可存在但不會建立向量索引。
- Qdrant 內容會隨最新合併版本更新。
- 刪除檔案後，對應索引會同步移除。
- Hermes 的 MCP 寫入流程可正常運作。

## 10. 待確認項目
- Agent 產生的內容是否需要先經人工審核。
- 主分支名稱是否固定為 `main`。
- Qdrant collection 命名規則。
- metadata 需要保留到什麼程度。
- 是否需要版本回溯與歷史查詢。

# 知識庫系統 PRD v2

## 1. 文件資訊
- 文件名稱：知識庫系統 PRD v2
- 目標：定義知識庫系統的產品需求、更新流程、系統邊界與驗收標準
- 適用對象：產品、工程、維運、內容編輯與使用者協作流程

## 2. 產品目標
建立一套以 `GitLab` 為唯一正式來源的知識庫系統，讓知識內容可以透過人工流程或 `agent` 協作流程持續更新，並在 `merge` 後自動完成向量索引更新與查詢準備。

### 2.1 核心目標
- 讓知識內容有清楚的版本控管與可追蹤性。
- 讓內容更新後可自動同步到索引系統。
- 讓 `agent` 能協助整理 `md`，但不破壞正式版本來源。
- 讓 `Knowledge Base API` 能以 webhook 方式維持知識庫更新。

## 3. 問題定義
目前需要一套可持續維護的知識庫機制，避免內容散落在多處、版本不一致、更新不同步或索引失效等問題。系統必須確保：
- 正式內容只有一個來源。
- 更新流程可審核、可回溯。
- 向量索引與正式內容同步。
- 附件可以保留，但不增加索引複雜度。

## 4. 使用情境

### 4.1 工程師編寫知識
- 工程師使用 `Obsidian` 撰寫或修改 Markdown 內容。
- 內容經過 GitLab 版控後，merge 到主分支成為正式版本。

### 4.2 使用者指令整理知識
- 使用者直接下指令給 `agent`。
- `agent` 協助整理、改寫或補充 Markdown。
- 修改結果仍需進入 GitLab 正式流程。

### 4.3 系統自動維護
- `Knowledge Base API` 透過 webhook 在 merge 後觸發同步。
- 同步後更新 embedding 與 `Qdrant`。

## 5. 產品範圍

### 5.1 納入範圍
- Markdown 文件管理
- GitLab 作為唯一正式來源
- merge 後自動同步
- chunk / embedding / Qdrant 索引更新
- 任務狀態、失敗重試與同步紀錄

### 5.2 不納入範圍
- 附件內容索引
- 圖片 OCR 或多媒體內容解析
- 非 GitLab 來源的正式知識內容

## 6. 內容與來源規則
- 主要內容格式為 `Markdown (.md)`。
- 附件可以附上，但不建立索引。
- `GitLab` 是唯一正式來源。
- `Obsidian` 是編寫工具，不是正式來源。
- `agent` 只能協助產出或整理內容，不能繞過 GitLab 正式流程。

## 7. 更新流程

### 7.1 方式 A：人工流程
1. 工程師在 Obsidian 編寫或修改內容。
2. Push 到 GitLab。
3. Merge 到主分支。
4. GitLab 發送 webhook。
5. Hermes 執行同步與索引更新。

### 7.2 方式 B：Agent 協作流程
1. User 下指令給 agent。
2. agent 協助整理、改寫、補充 `md`。
3. 內容進入 GitLab 版控流程。
4. Merge 到主分支後，由 Hermes 同步更新索引。

## 8. 產品需求

### 8.1 功能需求
- 系統需接收 GitLab merge webhook。
- 系統需辨識新增、修改、刪除的 Markdown 檔案。
- 系統需只對 Markdown 內容進行索引。
- 系統需支援 chunk、embedding、Qdrant upsert。
- 系統需支援刪除或失效索引資料同步移除。
- 系統需保留任務狀態、重試與錯誤紀錄。
- 系統需透過 Hermes 的 MCP 寫入能力完成資料寫入。

### 8.2 內容規則需求
- Markdown 為主要索引單位。
- 附件保留在 GitLab，但不進入索引。
- 內容更新以已 merge 的版本為準。
- 若 agent 產出內容，仍須經 GitLab 正式流程確認。

### 8.3 同步策略需求
- 採增量更新，不以全量重建為預設。
- merge 後需儘快完成索引更新。
- webhook 漏觸或同步失敗時，可由背景重試機制補償。

## 9. 系統角色

### 9.1 Obsidian
- 工程師編寫工具。
- 產出 Markdown 與附件。

### 9.2 GitLab
- 正式內容庫與版本來源。
- 變更合併後才視為正式發布。

### 9.3 Agent
- 協助使用者整理 Markdown。
- 不直接成為正式來源。

### 9.4 Knowledge Base API
- 接收 webhook 事件。
- 執行同步、embedding 與寫入流程。
- 管理任務與重試。

### 9.5 MCP
- Hermes 的寫入介面。
- 已完成，作為 Hermes 寫入流程的一部分。

### 9.6 Qdrant
- 儲存向量與 metadata。
- 供知識檢索與查詢使用。

## 10. 使用者流程

### 10.1 內容更新流程
1. 編寫或整理內容。
2. 進入 GitLab。
3. Merge 成正式版本。
4. API 同步並更新索引。

### 10.2 查詢準備流程
1. Markdown 切 chunk。
2. 產生 embedding。
3. 寫入 Qdrant。
4. 查詢端使用索引資料進行檢索。

## 11. 非功能需求
- 可追蹤：每次同步需保留紀錄。
- 可回溯：需可知道內容來源與版本。
- 可重跑：失敗任務需可補償。
- 可維運：需支援監控與告警擴充。
- 可擴充：未來若要支援附件索引，可另開模組擴展。

## 12. 資料設計原則
- Qdrant 至少保存以下 metadata：
  - 檔案路徑
  - 檔名
  - chunk id
  - commit hash
  - 更新時間
  - 文件標題
- 刪除、改名、搬移需同步更新索引資料。
- 所有索引資料都必須可追溯到 GitLab 版本。

## 13. 例外與風險
- Webhook 失敗可能導致更新延遲，需由背景重試機制補償。
- 同步失敗可能導致索引與正式內容短暫不一致，需有重試機制。
- 若內容沒有經 GitLab merge，則不得成為正式索引來源。
- 附件若日後要索引，需另外定義解析與安全流程。

## 14. 驗收標準
- merge 到主分支後，Hermes 可正常收到 webhook。
- Hermes 可從 GitLab 取得最新內容並處理變更檔案。
- 只有 `.md` 會進入索引流程。
- 附件可存在但不會建立索引。
- Qdrant 內容會隨最新合併版本更新。
- 刪除檔案後，對應索引會同步移除。
- Hermes 的 MCP 寫入流程可正常運作。
- 背景重試機制可補抓漏掉的更新或失敗任務。

## 15. 開發拆分

### 15.1 第一階段
- 定義 GitLab webhook 與 merge 事件。
- 完成 Hermes 拉取與變更辨識。
- 完成 Markdown chunk 與 embedding。

### 15.2 第二階段
- 完成 Qdrant upsert 與刪除。
- 補上重試、狀態與監控。
- 接入 API MCP 寫入能力。

### 15.3 第三階段
- 完善 agent 協作流程。
- 整理文件與維運規範。

## 16. 待確認項目
- 主分支名稱是否固定為 `master`。
- Agent 產出內容是否需要人工審核。
- Qdrant collection 命名規則。
- metadata 細節保留程度。
